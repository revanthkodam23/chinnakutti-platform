import type { AudioNarrationRequest } from "@/types/content";

export async function generateAudioNarration(request: AudioNarrationRequest) {
  void request;
  throw new Error("Audio narration generation is not configured yet.");
}
